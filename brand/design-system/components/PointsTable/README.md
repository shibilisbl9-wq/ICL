The league standings: position, team, played, won, lost, net run rate, points.

- Consumer provides: rows sorted by points then NRR; mark qualifying rows with `class="q"` (position number turns `imama-green`).
- Wrap the table in `icl-table-wrap` so it scrolls sideways on phones instead of squeezing.
- NRR always shows its sign and three decimals (+0.388, −1.190), with a true minus sign.
- Points column is the only bold number in the row.

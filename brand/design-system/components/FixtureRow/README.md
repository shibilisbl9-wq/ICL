One upcoming match in a schedule list: date block, the two teams, time and venue, and one action.

- Consumer provides: day number, 3-letter month, both team names, match number, start time, venue, and an optional action.
- Stack rows with `space-3` gap; group by week under a `label-caps` eyebrow.
- Times in 12-hour format with lowercase am/pm ("7:30 pm"), local UAE time.
- On narrow screens the action may drop below; keep the date block.
